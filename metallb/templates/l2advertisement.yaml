apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: {{ .Values.l2Advertisement.name }}
spec:
  ipAddressPools:
    {{- range .Values.l2Advertisement.pools }}
    - {{ . }}
    {{- end }}
