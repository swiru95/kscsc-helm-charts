CoreDNS custom configuration deployed for zone "{{ .Values.zone }}".

Hosts resolved to {{ .Values.ingressIP }} (nginx):
{{- range .Values.hosts.nginx }}
  - {{ . }}
{{- end }}

Hosts resolved to {{ .Values.envoyIP }} (Envoy Gateway):
{{- range .Values.hosts.envoy }}
  - {{ . }}
{{- end }}

To verify DNS resolution from inside the cluster:

  kubectl run dns-test --rm -it --restart=Never \
    --overrides='{"spec":{"tolerations":[{"key":"nvidia.com/gpu","operator":"Exists","effect":"NoSchedule"}]}}' \
    --image=busybox -- nslookup {{ (index .Values.hosts.envoy 0) }}

To check the configmap:

  kubectl -n kube-system get configmap coredns-custom -o yaml
