apiVersion: v1
kind: Service
metadata:
  name: {{ include "splunk.fullname" . }}-web
  labels:
    {{- include "splunk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "splunk.selectorLabels" . | nindent 4 }}
