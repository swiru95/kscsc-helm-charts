apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ollama.fullname" . }}
  labels:
    app: {{ include "ollama.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: {{ include "ollama.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ollama.name" . }}
    spec:
      {{- if .Values.gpu.enabled }}
      {{- if .Values.gpu.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.gpu.nodeSelector | indent 8 }}
      {{- end }}
      {{- if .Values.gpu.tolerations }}
      tolerations:
{{ toYaml .Values.gpu.tolerations | indent 8 }}
      {{- end }}
      {{- if .Values.gpu.affinity }}
      affinity:
{{ toYaml .Values.gpu.affinity | indent 8 }}
      {{- end }}
      {{- else }}
      {{- if .Values.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.nodeSelector | indent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations:
{{ toYaml .Values.tolerations | indent 8 }}
      {{- end }}
      {{- if .Values.affinity }}
      affinity:
{{ toYaml .Values.affinity | indent 8 }}
      {{- end }}
      {{- end }}
      {{- if .Values.runtimeClassName }}
      runtimeClassName: {{ .Values.runtimeClassName | quote }}
      {{- end }}
      {{- if .Values.podSecurityContext }}
      securityContext:
{{ toYaml .Values.podSecurityContext | indent 8 }}
      {{- end }}
      initContainers:
        - name: init-ollama-home
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          command: ['mkdir', '-p', '/root/.ollama']
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
      containers:
        - name: ollama
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          args:
{{ toYaml .Values.commandArgs | indent 12 }}
          {{- $res := .Values.resources | default dict }}
          {{- $limits := $res.limits | default dict }}
          {{- if .Values.gpu.enabled }}
          {{- $_ := set $limits .Values.gpu.resourceName .Values.gpu.count }}
          {{- end }}
          {{- $_ := set $res "limits" $limits }}
          resources:
{{ toYaml $res | indent 12 }}
          lifecycle:
            postStart:
              exec:
                command: ["/bin/sh", "-c", "nohup sh -c 'sleep 30 && (ollama pull {{ .Values.model.name }} 2>&1 && echo \"✓ Model {{ .Values.model.name }} is ready\" || echo \"✗ Model pull failed\") > /tmp/model-pull.log 2>&1 &' > /dev/null 2>&1 &"]
          env:
            - name: MODEL_NAME
              value: "{{ .Values.model.name }}"
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
      volumes:
        - name: models
{{- if .Values.pvc.enabled }}
          persistentVolumeClaim:
            claimName: {{ if .Values.pvc.name }}{{ .Values.pvc.name }}{{ else }}{{ include "ollama.fullname" . }}-models{{ end }}
{{- else }}
          emptyDir: {}
{{- end }}
