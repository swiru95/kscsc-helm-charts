apiVersion: v2
name: falco
description: Envoy Gateway routing for the Falco / Falcosidekick UI runtime-security stack
type: application
version: 0.1.0
# Tracks the upstream falcosecurity/falco chart this route was written against.
appVersion: "0.44.1"
